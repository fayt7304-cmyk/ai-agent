{
  "name": "mistral-agent-chat-worker",
  "version": "8.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute mistral-agent-chat-db --file=./schema.sql --local",
    "db:migrate:remote": "wrangler d1 execute mistral-agent-chat-db --file=./schema.sql --remote"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0",
    "@cloudflare/workers-types": "^4.20240925.0"
  }
}
